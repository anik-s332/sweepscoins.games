// @ts-nocheck
import { content } from "@/content";
import StaticContentPage from "../Common/StaticContentPage";

const TermsConditions = () => {
    return <StaticContentPage pageTitle={content.termsConditions.pageTitle} blocks={content.termsConditions.blocks} />;
}

export default TermsConditions;
