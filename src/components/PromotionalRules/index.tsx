// @ts-nocheck
import { content } from "@/content";
import StaticContentPage from "../Common/StaticContentPage";

const PromotionalRules = () => {
    return <StaticContentPage pageTitle={content.promotionalRules.pageTitle} blocks={content.promotionalRules.blocks} />;
}

export default PromotionalRules;
